<Page x:Class="LabDesktop.Views.BatchesListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Контроль готовой продукции">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Контроль качества готовой продукции" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Управление испытаниями партий" Foreground="#BDC3C7" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Таблица 1: Партии, ожидающие контроля (НЕТ тестов) -->
        <Border Grid.Row="1" Background="White" Padding="10" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📋 Партии, ожидающие контроля (нет тестов)" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="#E67E22"/>
                <DataGrid x:Name="dgPendingBatches" AutoGenerateColumns="True" 
                          IsReadOnly="True" SelectionMode="Single"
                          SelectionChanged="DgPendingBatches_SelectionChanged"
                          Height="180" BorderThickness="1" BorderBrush="#DDD"/>
            </StackPanel>
        </Border>

        <!-- Таблица 2: Партии с результатами (ЕСТЬ тесты) -->
        <Border Grid.Row="2" Background="White" Padding="10" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="✅ Партии с результатами испытаний (есть тесты)" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Foreground="#27AE60"/>
                <DataGrid x:Name="dgCompletedBatches" AutoGenerateColumns="True" 
                          IsReadOnly="True" SelectionMode="Single"
                          SelectionChanged="DgCompletedBatches_SelectionChanged"
                          Height="180" BorderThickness="1" BorderBrush="#DDD"/>
            </StackPanel>
        </Border>

        <!-- Результаты испытаний выбранной партии -->
        <Border Grid.Row="3" Background="White" Padding="15" CornerRadius="8" Visibility="Collapsed" x:Name="panelDetails">
            <StackPanel>
                <TextBlock x:Name="txtBatchTitle" FontWeight="Bold" FontSize="14" Margin="0,0,0,10" Background="#F0F0F0" Padding="10"/>
                <DataGrid x:Name="dgTests" AutoGenerateColumns="True" IsReadOnly="True" 
                          Height="150" Margin="0,0,0,15" BorderThickness="1" BorderBrush="#DDD"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="🔬 Добавить тест" Background="#3498DB" Foreground="White" 
                            Width="130" Height="35" Margin="0,0,10,0" Cursor="Hand" Click="AddTest_Click"/>
                    <Button Content="✅ Завершить контроль" Background="#27AE60" Foreground="White" 
                            Width="150" Height="35" Cursor="Hand" Click="CompleteQC_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="4" x:Name="txtNoSelection" Text="👈 Выберите партию из списка выше" 
                   Foreground="#7F8C8D" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center" Visibility="Visible"/>
    </Grid>
</Page>