<Page x:Class="AgroSintez.Views.ProductionBatchesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Производственные партии">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Производственные партии" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="Управление производственными партиями" Foreground="#7F8C8D" Margin="0,5,0,15"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="➕ Создать" Style="{StaticResource PrimaryButton}" Click="AddBatch_Click" Margin="0,0,10,0"/>
                    <Button Content="🔄 Обновить" Style="{StaticResource PrimaryButton}" Click="Refresh_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid x:Name="dgBatches" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                  CanUserResizeColumns="True" CanUserReorderColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding id}" Width="50"/>
                <DataGridTextColumn Header="Номер партии" Binding="{Binding batch_number}" Width="150"/>
                <DataGridTextColumn Header="ID заказа" Binding="{Binding order_id}" Width="80"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding status}" Width="120"/>
                <DataGridTextColumn Header="Факт, кг" Binding="{Binding actual_quantity_kg}" Width="80"/>
                <DataGridTextColumn Header="Время старта" Binding="{Binding start_time}" Width="150"/>
                <DataGridTextColumn Header="Время окончания" Binding="{Binding end_time}" Width="150"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding created_date}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>